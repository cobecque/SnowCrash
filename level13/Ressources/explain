ls -la, on trouve un binaire.
objdump du binaire, on trouve les fonctions printf, getuid.
test du binaire, il attend l'uid 4242.
gdb le binaire, disass le main.
breakpoint sur le debut, run, jump sur la partie du main accessible avec le bon uid.
on obtient le token.
