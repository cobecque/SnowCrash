ls -la, on trouve un binaire.
objdump -s, on voit system et asprintf en fonction. Dans les rodata on voit la variable env LOGNAME et un echo %s immonde, on export LOGNAME='random;getflag' et on execute.
pas de flag once again.
